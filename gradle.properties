# Point Gradle at its cached user home on E: (same as Android builds use)
org.gradle.caching=true
org.gradle.parallel=true

# JVM args for the Gradle daemon
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
